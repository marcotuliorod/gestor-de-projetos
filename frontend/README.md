# Frontend (placeholder)

O frontend ainda **não foi iniciado**. A UI segue como fonte de verdade o
link Claude Design e o `design-brief-gestor-de-projetos.md` (fallback textual),
que ainda não estão neste repositório.

Quando os documentos de design forem adicionados, definiremos aqui:

- Framework do SPA (React/Vite ou equivalente) — decisão **em aberto**.
- Base PWA (manifest + service worker), mobile-first.
- Temas: light, dark e "de acordo com o sistema".

Por ora, o backend expõe a API em `/api/` (ver README raiz) para consumo futuro
por este frontend.
